# WP-10 — Governed router + route recording

**Epic:** E3 Model & agent plane · **Sprint:** Sprint 2 · **Package:** `kca/platform/router/` · **Depends on:** WP-09
**Branch:** `wp-10-governed-router-route`

## Scope

Decision path: task class, data sensitivity, required capability, permitted deployment boundary, cost/latency budget, selected model. Routing rules are versioned config; route written to ledger.

## Acceptance criteria (PR checklist — all must pass)

- [ ] Confidential task class can never route out-of-boundary (test)
- [ ] Every call has a recorded, replayable route

## Rules reminder

Follow CLAUDE.md strictly: LLM in L3/L4 only · no LLM-computed numbers · retrieval takes as_of + caller identity, permission filter pre-ranking, fail-closed · every model call in the ledger · cross-package via contracts/ only · no cloud SDKs in core · tests ship with the WP.

## Out of scope

Anything not listed above. Do not refactor other packages, add dependencies beyond pyproject, or change contracts/ schemas without flagging it in the PR description.
