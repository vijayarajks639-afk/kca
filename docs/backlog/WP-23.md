# WP-23 — Cross-domain discovery index

**Epic:** E6 Portability proof · **Sprint:** Sprint 5 · **Package:** `kca/platform/discovery/` · **Depends on:** WP-06, WP-22
**Branch:** `wp-23-cross-domain-discovery`

## Scope

Thin metadata/entity pointers across domains — no content; intent classifier proposes domains; low confidence widens search or returns reason-coded result.

## Acceptance criteria (PR checklist — all must pass)

- [ ] Authorisation still enforced at each domain boundary
- [ ] Cross-domain test question finds op-risk evidence from a credit query

## Rules reminder

Follow CLAUDE.md strictly: LLM in L3/L4 only · no LLM-computed numbers · retrieval takes as_of + caller identity, permission filter pre-ranking, fail-closed · every model call in the ledger · cross-package via contracts/ only · no cloud SDKs in core · tests ship with the WP.

## Out of scope

Anything not listed above. Do not refactor other packages, add dependencies beyond pyproject, or change contracts/ schemas without flagging it in the PR description.
