# WP-06 — Hybrid retrieval with pre-ranking permission filter

**Epic:** E2 Knowledge & context layer · **Sprint:** Sprint 1 · **Package:** `kca/platform/retrieval/` · **Depends on:** WP-05, WP-08
**Branch:** `wp-06-hybrid-retrieval-with`

## Scope

Lexical (tsvector) + vector (pgvector) fusion; authz filter applied BEFORE ranking; every hit carries source version + effective dates.

## Acceptance criteria (PR checklist — all must pass)

- [ ] Test proves unauthorised docs are absent from the candidate set (not merely down-ranked)
- [ ] P95 latency < 500ms on the fixture corpus
- [ ] Every hit carries version + effective dates

## Rules reminder

Follow CLAUDE.md strictly: LLM in L3/L4 only · no LLM-computed numbers · retrieval takes as_of + caller identity, permission filter pre-ranking, fail-closed · every model call in the ledger · cross-package via contracts/ only · no cloud SDKs in core · tests ship with the WP.

## Out of scope

Anything not listed above. Do not refactor other packages, add dependencies beyond pyproject, or change contracts/ schemas without flagging it in the PR description.
