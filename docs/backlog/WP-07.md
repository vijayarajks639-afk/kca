# WP-07 — Semantic service — shared core + credit extension

**Epic:** E2 Knowledge & context layer · **Sprint:** Sprint 1 · **Package:** `platform/semantics/` · **Depends on:** WP-02
**Branch:** `wp-07-semantic-service-shared`

## Scope

Glossary/ontology API. Register "exposure" as shared parent with CreditRisk.Exposure (EAD) and Finance.Exposure (carrying value) extensions; context-based resolution rule (department, role, application); unresolved context abstains.

## Acceptance criteria (PR checklist — all must pass)

- [ ] Resolution works by context
- [ ] Ambiguous context returns AMBIGUOUS_TERM, never a guess
- [ ] Every term has a named steward and effective date

## Rules reminder

Follow CLAUDE.md strictly: LLM in L3/L4 only · no LLM-computed numbers · retrieval takes as_of + caller identity, permission filter pre-ranking, fail-closed · every model call in the ledger · cross-package via contracts/ only · no cloud SDKs in core · tests ship with the WP.

## Out of scope

Anything not listed above. Do not refactor other packages, add dependencies beyond pyproject, or change contracts/ schemas without flagging it in the PR description.
