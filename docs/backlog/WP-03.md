# WP-03 — Docker Compose: Postgres 16 + pgvector + Keycloak

**Epic:** E1 Foundation · **Sprint:** Sprint 0 · **Package:** `infra/` · **Depends on:** WP-01
**Branch:** `wp-03-docker-compose-postgres`

## Scope

One-command local stack; Alembic migrations wired; Keycloak realm seeded with roles credit-officer, domain-steward, auditor, op-risk-investigator, unauthorised-user. (Compose + realm exist in starter — wire Alembic, healthchecks, make targets.)

## Acceptance criteria (PR checklist — all must pass)

- [ ] `make up` yields healthy stack in under 2 minutes
- [ ] OIDC token for each role resolvable via direct grant
- [ ] `alembic upgrade head` and `downgrade` both work

## Rules reminder

Follow CLAUDE.md strictly: LLM in L3/L4 only · no LLM-computed numbers · retrieval takes as_of + caller identity, permission filter pre-ranking, fail-closed · every model call in the ledger · cross-package via contracts/ only · no cloud SDKs in core · tests ship with the WP.

## Out of scope

Anything not listed above. Do not refactor other packages, add dependencies beyond pyproject, or change contracts/ schemas without flagging it in the PR description.
