# WP-13 — Credit Risk DIP package

**Epic:** E4 Credit Risk DIP + the worked journey · **Sprint:** Sprint 3 · **Package:** `kca/dips/credit-risk/` · **Depends on:** WP-04, WP-07
**Branch:** `wp-13-credit-risk-dip`

## Scope

Six asset classes as versioned content/config: semantic extension, governed corpus, data contracts, tool grants, agent instructions + abstention rules, golden sets.

## Acceptance criteria (PR checklist — all must pass)

- [ ] Validates against the DIP contract schema
- [ ] Published contract (owner, freshness/quality SLOs, access policy, eval gate) renders from the package

## Rules reminder

Follow CLAUDE.md strictly: LLM in L3/L4 only · no LLM-computed numbers · retrieval takes as_of + caller identity, permission filter pre-ranking, fail-closed · every model call in the ledger · cross-package via contracts/ only · no cloud SDKs in core · tests ship with the WP.

## Out of scope

Anything not listed above. Do not refactor other packages, add dependencies beyond pyproject, or change contracts/ schemas without flagging it in the PR description.
